# info-git

## Logout git (MacOS)
```sh
git credential-osxkeychain erase \
host=github.com  \
protocol=https   
```

## Set global info
```sh
git config --global user.name ""
git config --global user.email ""
```
